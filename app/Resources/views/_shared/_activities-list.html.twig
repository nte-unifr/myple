<div class="list-group list-group-flush">
  {% for activity in activities %}
    <a href="{{ path('activities_show', {'id': activity.id}) }}" class="list-group-item list-group-item-action {% if activity.id == active %}active{% endif %}">
      {{ activity }}
    </a>
  {% endfor %}
</div>