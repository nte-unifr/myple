<div class="card mb-2">
  <div class="card-header d-flex justify-content-between align-items-center">
    {{ 'task_activities'|trans }} <span class="badge badge-pill badge-primary">{{ activities | length }}</span>
  </div>
  {% include '_shared/_activities-list.html.twig' %}
</div>